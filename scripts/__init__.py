"""Standalone CLI scripts (smoke tests, ops helpers).

Not part of the runtime application package — kept here so they can be
invoked via ``python -m scripts.<name>`` while sharing the same venv.
"""
