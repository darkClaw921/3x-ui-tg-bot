# app/services/__init__.py

Пакет бизнес-логики приложения. Подмодули: promos (валидация/скидка/redeem), billing (расчёт цены + Stars invoice), subscriptions (единая точка создания/продления подписки через xui+db). Сервисы оркестрируют репозитории и xui-клиент, держат код хэндлеров тонким.
