# app/xui/__init__.py

Пакет 3x-ui REST-клиента. Re-exports: XuiClient, XuiError, get_xui_client, close_xui_client из app.xui.client. Подмодули: client (HTTP+singleton), inbounds (list/get inbound), clients (add/update/del/getTraffics), links (vless URI, sub URL, QR PNG).
