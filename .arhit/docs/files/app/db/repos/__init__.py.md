# app/db/repos/__init__.py

Маркер пакета app.db.repos. Документирует контракт репозиториев: каждая функция — async, принимает aiosqlite.Connection первым аргументом (DI). Никакого ORM, чистый SQL. Жизненный цикл соединения принадлежит вызывающему коду (через app.db.engine.get_conn).
